# `amlt edit`

Edits the description (or the config) of an experiment.

**Parameters:**

  - `EXP` (multiple)
  - `-c`, `--config`: edit the whole config file, not just the description
  - `--json-help`: Print command help as structured JSON and exit.
